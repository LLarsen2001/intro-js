//loop through array in JS
let nums =[10,20,30,40]

const isGreaterThan10 = (num=>{
    if(num>=10){
        console.log(num + 'is greater then ten')
    } else {
        console.log(num + 'is less than ten')
    }
    
})

